{% extends 'base.html.twig' %}

{% block body %}
    <h1>
        {{ podcast.title }}
    </h1>
    <div class="subscribe">
        {{ render(controller('App\\Controller\\Site\\HomeController::subscribe'))}}
    </div>
    <div class="description">
        <p>{{ podcast.description }}</p>
    </div>

{% endblock %}
